双重检查:
  优缺点：
    1> Double-Check 概念是多线程中常常使用到的，如代码中所示，我们进行了两次 if(instance==null) 的判断，这样就可以保证线程安全了
    2> 实例化代码只需要执行一次，后面再次访问，判断 if(instance==null)，直接返回实例化对象，也避免了反复进行方法同步。
    3> 解决了线程安全，和 Lazy loading 的问题，效率更高
    4> 结论： 实际开发中，建议使用这种方式




